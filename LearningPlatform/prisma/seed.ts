/**
 * Prisma seed stub - the actual seeding is done by src/payload/seed.ts
 * This file exists because package.json db:seed script looks for prisma/seed.ts
 * but we keep the actual implementation in src/payload/seed.ts for better organization
 */
console.log('[INFO] Prisma seed stub - skipping (use cms:seed for actual seeding)')
process.exit(0)
